{
  "name": "open-snapora-monorepo",
  "version": "1.0.2",
  "private": true,
  "description": "Cross-framework desktop screenshot & annotation suite for Electron, Tauri, and Webview",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* run build",
    "dev:overlay": "pnpm --filter @open-snapora/overlay dev",
    "demos:electron": "pnpm --filter demo-electron start",
    "demos:tauri": "pnpm --filter demo-tauri tauri dev",
    "demos:tauri:check": "cargo check --manifest-path demos/tauri/src-tauri/Cargo.toml",
    "demos:tauri:build": "pnpm --filter demo-tauri build",
    "demos:tauri:exe": "pnpm --filter demo-tauri run build:exe",
    "demo:electron": "pnpm --filter demo-electron start",
    "demo:tauri": "pnpm --filter demo-tauri tauri dev",
    "demo:tauri:exe": "pnpm --filter demo-tauri run build:exe",
    "check:all": "pnpm run build && pnpm run typecheck && cargo check --manifest-path crates/tauri-plugin-snapora/Cargo.toml && cargo check --manifest-path demos/tauri/src-tauri/Cargo.toml",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r exec rimraf dist",
    "publish:dry-run": "node scripts/publish-packages.mjs --dry-run",
    "publish:packages": "node scripts/publish-packages.mjs"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "allowBuilds": {
      "esbuild": true,
      "electron": true
    },
    "onlyBuiltDependencies": [
      "esbuild",
      "electron"
    ]
  },
  "dependencies": {
    "desklog": "^1.0.2"
  }
}
